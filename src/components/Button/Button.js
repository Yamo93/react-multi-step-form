import React from 'react';
import styles from './Button.module.scss';

const button = (props) => <input type="submit" value="Continue" className={styles.contact__formSubmit} />;

export default button;